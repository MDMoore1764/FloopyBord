export enum GameState {
	/**The game is running. */
	Running,
	/**The game is paused. */
	Paused,
	/**The level is completed and the player is waiting for a new level to begin. */
	LevelCompleted,
	/**The game has ended. */
	GameOver
}
